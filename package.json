{
  "name": "gliojs",
  "version": "0.0.7",
  "description": "Detects if the mouse of a user leaves the viewport / document borders of your website — and when this happens, trigger your callback",
  "main": "dist/glio.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "browserify -t [ babelify --presets [ @babel/preset-env ] ] src/index.js -o dist/glio.js",
    "dev": "watchify src/index.js -v -o dist/glio.js"
  },
  "keywords": [
    "gliojs",
    "mousemove",
    "screencorner",
    "move",
    "exit"
  ],
  "homepage": "http://luisvinicius167.github.io/gliojs",
  "author": "Michael Tessier",
  "license": "MIT",
  "browserify": {
    "transform": [
      "babelify"
    ]
  },
  "devDependencies": {
    "@babel/core": "7.1.6",
    "@babel/preset-env": "7.1.6",
    "babelify": "10.0.0",
    "browserify": "16.2.3",
    "watchify": "3.11.0"
  },
  "dependencies": {
    "js-cookie": "2.2.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/MichaelTessier/gliojs.git" 
  },
  "bugs": {
    "url": "https://github.com/MichaelTessier/gliojs/issues"
  }
}
